# N3_PESQUISA_OPERACIONAL

Para rodar o código deve entrar na pasta em que o arquivo está e rodar os seguintes código:

```python -m pip install streamlit numpy pandas plotly``` \
```python -m streamlit run simulador_ev.py```
